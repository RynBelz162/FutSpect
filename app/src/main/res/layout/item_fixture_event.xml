<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
                name="event"
                type="com.belzsoftware.futspect.model.fixture.Event" />

        <variable
                name="homeTeamId"
                type="int" />

        <variable
                name="awayTeamId"
                type="int" />
    </data>

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_margin="@dimen/margin_small"
            bind:homeTeam="@{homeTeamId}"
            bind:eventTeam="@{event.team.id}">

        <com.google.android.material.textview.MaterialTextView
                android:id="@+id/textView_fixtureEvent_time"
                style="@style/TextAppearance.MaterialComponents.Body1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="59"
                android:text="@{String.valueOf(event.time.elapsed)}"/>

        <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin_small"
                android:contentDescription="@string/fixtureEvent_contentDescription"
                tools:src="@drawable/ic_soccer"
                setEventIcon="@{event.detail}"/>

        <com.google.android.material.textview.MaterialTextView
                android:id="@+id/textView_fixtureEvent_player"
                style="@style/TextAppearance.MaterialComponents.Body1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin_small"
                tools:text="P. Aubameyang"
                android:text="@{event.player.name}"/>

    </LinearLayout>
</layout>